management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics

  endpoint:
    health:
      show-details: always

  metrics:
    web:
      server:
        auto-time-requests: true

    distribution:
      percentiles-histogram:
        http:
          server:
            requests: true

spring:
  security:
    user:
      name: admin
      password: admin